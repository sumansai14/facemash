<h1>Profile#show</h1>
<p>Find me in app/views/profile/show.html.erb</p>
<%= @user.username %>

<div class="profilepic">
<div id="test">
	<%= link_to (image_tag @profile.picture.url(:small), :id => 'test')	, username_path(@user.username)  %>

</div>
	<% if current_user.username==@user.username -%>
		<%= form_for @profile, :html => {:multipart => true },:remote => true,:url => {:controller => 'profile',:action => 'update' } do |f| -%>
		<div id="file">
			<%= f.file_field :picture, :label => 'test' %>
		</div>
		
		<%= f.check_box :image_delete, :label => 'Delete Image' %>
		<%= submit_tag 'update' %>
		
	<% end -%>
	<% else -%>
	
	<% end -%>	
	
</div>





